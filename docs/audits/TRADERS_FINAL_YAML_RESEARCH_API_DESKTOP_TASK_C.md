# Task C — research, API and Desktop authority re-audit

```text
STATUS = FAIL_UNPROVENANCED_VALUES_REMAIN
RESEARCH_AND_SCRIPT_UNKNOWN = 16
DESKTOP_UNKNOWN = 27
API_LOCAL_POLICY_DEFAULTS = NOT_PROVEN_ZERO
DESKTOP_POLICY_VALUES = NOT_PROVEN_ZERO
TEST_FIXTURE_AUTHORITY_LEAKS = 0_IN_REPAIRED_PG_SCOPE
```

Research finalization now consumes YAML-derived active geometry/economics and
shadow cohorts and its focused regression passes. The readonly API projects the
persisted Set #2 frozen snapshot; active commission evidence is a real Binance
account-commission snapshot, while spread/depth are public Binance inputs and
slippage/adverse reserve retain YAML provenance. The full Desktop suite is
green, and fixture repair obtains current risk/cost identity from the resolver.

The independent scan nevertheless leaves 12 script, four research-package and
27 Desktop transport/local-limit candidates requiring trace. Missing authority
has not been converted into invented UI trading values, but zero independent UI
or research policy authority is not yet proven. No API or Desktop deployment was
performed.
